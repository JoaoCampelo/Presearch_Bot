config = {
    "delay": 4,  # Delay between searches (seconds)
    "random": True,  # Loop through the search.txt words randomly
    "searches_count": 50,  # Only if random is True
    "profile_number": 1,  # The Chrome's profile number starting from 1
}


